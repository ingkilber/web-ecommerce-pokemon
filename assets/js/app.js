import { getActiveCards, getAllCards, createCard, updateCard, uploadCardImage } from './services/cards-service.js';
import { login, logout, getAdminProfile } from './services/auth-service.js';
const money=new Intl.NumberFormat('es-AR',{style:'currency',currency:'ARS',maximumFractionDigits:0}),$=s=>document.querySelector(s),escape=v=>{const n=document.createElement('span');n.textContent=v||'';return n.innerHTML},cartKey='ghostnoxCart',cart=JSON.parse(localStorage.getItem(cartKey)||'[]');
function renderCart(){const count=$('[data-cart-count]'),items=$('[data-cart-items]'),total=$('[data-cart-total]');if(!count)return;count.textContent=cart.reduce((s,i)=>s+i.quantity,0);total.textContent=money.format(cart.reduce((s,i)=>s+i.price*i.quantity,0));items.innerHTML=cart.length?cart.map(i=>`<p><b>${escape(i.name)} × ${i.quantity}</b><br><small>${money.format(i.price*i.quantity)}</small></p>`).join(''):'<p>Tu carrito está vacío.</p>'}
function openCart(open){$('[data-cart-panel]')?.classList.toggle('open',open);$('[data-overlay]')?.classList.toggle('visible',open)}
function addToCart(card,quantity=1){if(card.stock<1)return;const amount=Math.min(Math.max(+quantity||1,1),card.stock),item=cart.find(i=>i.id===card.id);if(item)item.quantity=Math.min(item.quantity+amount,card.stock);else cart.push({id:card.id,name:card.name,price:+card.price,quantity:amount});localStorage.setItem(cartKey,JSON.stringify(cart));renderCart();openCart(true)}
function setupCart(){renderCart();$('[data-cart-button]')?.addEventListener('click',()=>openCart(true));$('[data-close-cart]')?.addEventListener('click',()=>openCart(false));$('[data-overlay]')?.addEventListener('click',()=>openCart(false));$('[data-checkout-button]')?.addEventListener('click',()=>{if(!cart.length)return;const lines=cart.map(i=>`• ${i.name} × ${i.quantity} — ${money.format(i.price*i.quantity)}`),total=cart.reduce((s,i)=>s+i.price*i.quantity,0);window.open(`https://wa.me/5491170591196?text=${encodeURIComponent(['Hola GHOSTNOX, quiero realizar esta compra:','',...lines,'',`Total: ${money.format(total)}`].join('\n'))}`,'_blank','noopener')})}
function cardMarkup(card,single=false){const soldOut=card.stock<1,image=card.image_url?`<img src="${card.image_url}" alt="${escape(card.name)}">`:'';if(!single)return `<article class="product sealed-product ${soldOut?'sold-out':''}" data-card-id="${card.id}"><div class="product-image ${card.category}">${image}${soldOut?'<span class="stock-badge">Agotado</span>':''}</div><small>${escape(card.rarity)}</small><h3>${escape(card.name)}</h3><div><b>${money.format(card.price)}</b><button data-add-card="${card.id}" ${soldOut?'disabled':''}>${soldOut?'Agotado':'Comprar'}</button></div></article>`;const choices=Array.from({length:Math.min(Math.max(card.stock,1),10)},(_,i)=>`<option value="${i+1}">${i+1}</option>`).join('');return `<article class="single" data-card-id="${card.id}"><button class="heart" type="button" aria-label="Guardar carta">♡</button><div class="single-art ${card.category}">${image}</div><div><p>${escape(card.collection)}${card.card_number?` · ${escape(card.card_number)}`:''}</p><h3>${escape(card.name)}</h3><em>${escape(card.language)} · ${escape(card.condition)}</em><footer><b>${money.format(card.price)}</b><span><select data-quantity aria-label="Cantidad" ${soldOut?'disabled':''}>${choices}</select><button data-add-card="${card.id}" ${soldOut?'disabled':''}>${soldOut?'Agotado':'Comprar'}</button></span></footer></div></article>`}
function bindCards(container,cards){container.onclick=e=>{if(e.target.matches('.heart'))return e.target.classList.toggle('saved');if(e.target.matches('[data-quantity]'))return;const article=e.target.closest('[data-card-id]'),card=cards.find(i=>i.id===article?.dataset.cardId);if(!card)return;if(e.target.closest('[data-add-card]'))addToCart(card,article.querySelector('[data-quantity]')?.value);else window.openCardDialog?.(card)}}
async function loadHomeCatalog(){const grid=$('.products');if(!grid)return;grid.innerHTML='<p>Cargando cartas...</p>';try{const cards=await getActiveCards(),featured=cards.filter(c=>c.featured),shown=(featured.length?featured:cards).slice(0,4);grid.innerHTML=shown.length?shown.map(c=>cardMarkup(c)).join(''):'<p>No hay cartas disponibles.</p>';bindCards(grid,shown)}catch{grid.innerHTML='<p>No fue posible cargar las cartas.</p>'}}
async function loadSingles(){const view=$('#singles-view');if(!view)return;view.innerHTML=`<section class="singles"><div class="singles-title"><div><p class="eyebrow">SINGLES</p><h2>La carta que falta<br>en tu colección.</h2><p>Cartas individuales verificadas para completar tu binder.</p></div><button data-toggle-filters>Filtros</button></div><div class="singles-layout"><aside><div class="filter-head"><b>Filtros</b><button data-clear>Limpiar</button></div><label>Buscar<input data-search placeholder="Nombre, número o expansión"></label><details open><summary>Expansión</summary><div data-collections></div></details><details open><summary>Precio</summary><div class="price"><input type="number" data-min placeholder="Mín."><input type="number" data-max placeholder="Máx."></div></details><label><input type="checkbox" data-stock> Solo disponibles</label></aside><div class="singles-main"><div class="single-toolbar"><span><b data-count>0</b> cartas encontradas</span><label>Ordenar <select data-sort><option value="featured">Destacadas</option><option value="low">Menor precio</option><option value="high">Mayor precio</option><option value="name">A-Z</option></select></label></div><p class="single-status">Cargando cartas...</p><div class="single-grid"></div><p class="empty" hidden>No encontramos cartas.</p><div class="pages" data-pages></div></div></div></section>`;try{const cards=await getActiveCards('single'),section=view.querySelector('.singles'),grid=view.querySelector('.single-grid'),state={query:'',collections:[],min:0,max:0,stock:false,sort:'featured',page:1},perPage=6,collections=[...new Set(cards.map(c=>c.collection).filter(Boolean))];view.querySelector('[data-collections]').innerHTML=collections.map(v=>`<label><input type="checkbox" data-collection value="${escape(v)}"> ${escape(v)}</label>`).join('');const render=()=>{let found=cards.filter(c=>(!state.query||`${c.name} ${c.collection} ${c.card_number}`.toLowerCase().includes(state.query))&&(!state.collections.length||state.collections.includes(c.collection))&&(!state.min||c.price>=state.min)&&(!state.max||c.price<=state.max)&&(!state.stock||c.stock>0));if(state.sort==='low')found.sort((a,b)=>a.price-b.price);if(state.sort==='high')found.sort((a,b)=>b.price-a.price);if(state.sort==='name')found.sort((a,b)=>a.name.localeCompare(b.name));const pages=Math.max(1,Math.ceil(found.length/perPage));state.page=Math.min(state.page,pages);const visible=found.slice((state.page-1)*perPage,state.page*perPage);view.querySelector('[data-count]').textContent=found.length;grid.innerHTML=visible.map(c=>cardMarkup(c,true)).join('');view.querySelector('.empty').hidden=!!visible.length;view.querySelector('[data-pages]').innerHTML=pages>1?Array.from({length:pages},(_,i)=>`<button class="${state.page===i+1?'active':''}" data-page="${i+1}">${i+1}</button>`).join(''):'';bindCards(grid,visible)};view.querySelector('.single-status').remove();section.oninput=e=>{if(e.target.matches('[data-search]'))state.query=e.target.value.toLowerCase();if(e.target.matches('[data-min]'))state.min=+e.target.value;if(e.target.matches('[data-max]'))state.max=+e.target.value;if(e.target.matches('[data-collection]'))state.collections=[...view.querySelectorAll('[data-collection]:checked')].map(i=>i.value);if(e.target.matches('[data-stock]'))state.stock=e.target.checked;state.page=1;render()};section.onchange=e=>{if(e.target.matches('[data-sort]')){state.sort=e.target.value;state.page=1;render()}};section.onclick=e=>{if(e.target.matches('[data-toggle-filters]'))section.classList.toggle('open');if(e.target.matches('[data-page]')){state.page=+e.target.dataset.page;render();window.scrollTo({top:0,behavior:'smooth'})}if(e.target.matches('[data-clear]')){Object.assign(state,{query:'',collections:[],min:0,max:0,stock:false,sort:'featured',page:1});section.querySelectorAll('input').forEach(i=>{i.checked=false;if(i.type!=='checkbox')i.value='' });render()}};render()}catch(error){view.querySelector('.single-status').textContent=`No fue posible cargar las cartas. ${error.message}`}}
function adminMarkup(){return `<section class="admin-login"><form id="supabase-login"><p class="eyebrow">GESTOR ADMINISTRADOR</p><h1>Iniciar sesión</h1><label>Email<input name="email" type="email" required></label><label>Contraseña<input name="password" type="password" required></label><button class="button">Ingresar</button><p class="form-message"></p></form></section><section id="admin-panel" hidden><div class="admin-heading"><div><p class="eyebrow">GESTOR ADMINISTRADOR</p><h1>Cartas Singles</h1><p>Gestioná las cartas publicadas en la tienda.</p></div><div><a class="quiet" href="singles.html">Ver Singles</a><button id="admin-logout" class="quiet">Salir</button></div></div><div class="admin-grid"><form id="admin-card-form" class="admin-form"><h2 data-form-title>Nueva carta</h2><input name="id" type="hidden"><label>Nombre<input name="name" required></label><label>Descripción<textarea name="description"></textarea></label><div class="form-row"><label>Precio (ARS)<input name="price" type="number" min="0" required></label><label>Stock<input name="stock" type="number" min="0" required></label></div><div class="form-row"><label>Categoría<select name="category"><option value="single">Single</option><option value="sealed">Sellado</option></select></label><label>Colección / set<input name="collection"></label></div><div class="form-row"><label>Número<input name="card_number"></label><label>Rareza<input name="rarity"></label></div><div class="form-row"><label>Idioma<input name="language" value="Español"></label><label>Estado<input name="condition" value="Casi perfecta"></label></div><label>Imagen<input name="image" type="file" accept="image/*"></label><label><input name="active" type="checkbox" checked> Activa</label><label><input name="featured" type="checkbox"> Destacada</label><button class="button">Guardar carta</button><button type="button" id="cancel-edit" hidden>Cancelar edición</button><p class="form-message" id="admin-message"></p></form><section class="admin-inventory"><div><h2>Inventario</h2><input id="admin-search" type="search" placeholder="Buscar carta"></div><p><b data-admin-count>0</b> cartas</p><ul id="admin-list"></ul></section></div></section>`}
async function setupAdmin(){const root=$('.admin-wrap');root.innerHTML=`<a class="brand" href="index.html">GHOSTNOX</a>${adminMarkup()}`;const loginForm=$('#supabase-login'),panel=$('#admin-panel'),loginBox=$('.admin-login'),form=$('#admin-card-form'),list=$('#admin-list'),search=$('#admin-search');let cards=[];const render=()=>{const found=cards.filter(c=>`${c.name} ${c.collection} ${c.card_number}`.toLowerCase().includes(search.value.toLowerCase()));$('[data-admin-count]').textContent=found.length;list.innerHTML=found.map(c=>`<li><img src="${c.image_url||''}" alt=""><span><b>${escape(c.name)}</b><small>${escape(c.collection)} · ${money.format(c.price)} · Stock ${c.stock} · ${c.active?'Activa':'Inactiva'}</small></span><button data-edit="${c.id}">Editar</button><button data-toggle="${c.id}">${c.active?'Desactivar':'Activar'}</button></li>`).join('')},refresh=async()=>{cards=await getAllCards();render()},show=async()=>{loginBox.hidden=true;panel.hidden=false;await refresh()};if(await getAdminProfile().catch(()=>null))await show();loginForm.onsubmit=async e=>{e.preventDefault();const message=loginForm.querySelector('.form-message');try{await login(new FormData(loginForm).get('email'),new FormData(loginForm).get('password'));if(!await getAdminProfile())throw Error('Tu cuenta no tiene permisos de administrador.');await show()}catch(error){message.textContent=error.message}};$('#admin-logout').onclick=async()=>{await logout();location.reload()};search.oninput=render;list.onclick=async e=>{const id=e.target.dataset.edit||e.target.dataset.toggle,card=cards.find(c=>c.id===id);if(!card)return;if(e.target.dataset.toggle){await updateCard(id,{active:!card.active});return refresh()}Object.entries(card).forEach(([key,value])=>{if(form.elements[key])form.elements[key].value=value??''});form.elements.active.checked=card.active;form.elements.featured.checked=card.featured;$('[data-form-title]').textContent=`Editar ${card.name}`;$('#cancel-edit').hidden=false};$('#cancel-edit').onclick=()=>{form.reset();form.elements.id.value='';$('[data-form-title]').textContent='Nueva carta';$('#cancel-edit').hidden=true};form.onsubmit=async e=>{e.preventDefault();const data=new FormData(form),image=data.get('image'),payload={name:data.get('name').trim(),description:data.get('description').trim(),price:+data.get('price'),stock:+data.get('stock'),category:data.get('category'),collection:data.get('collection').trim(),card_number:data.get('card_number').trim(),rarity:data.get('rarity').trim(),language:data.get('language').trim(),condition:data.get('condition').trim(),active:data.get('active')==='on',featured:data.get('featured')==='on'};try{if(image instanceof File&&image.size)payload.image_url=await uploadCardImage(image);if(data.get('id'))await updateCard(data.get('id'),payload);else await createCard(payload);$('#admin-message').textContent='Carta guardada correctamente.';$('#cancel-edit').click();await refresh()}catch(error){$('#admin-message').textContent=error.message}}}
setupCart();if(document.body.dataset.page==='admin')setupAdmin();if(document.body.dataset.page==='singles')loadSingles();if(!document.body.dataset.page)loadHomeCatalog();
